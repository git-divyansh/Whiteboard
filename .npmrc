# pnpm configuration
# Hoist nothing to the root by default for stricter dependency isolation,
# but allow common build tooling to resolve. Adjust as the project grows.
strict-peer-dependencies=false
auto-install-peers=true
# Keep the lockfile authoritative in CI.
prefer-frozen-lockfile=true
